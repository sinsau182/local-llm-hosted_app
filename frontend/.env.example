NEXT_PUBLIC_API_BASE_URL=https://breachlabz-nucbox-evo-x2.tail040b45.ts.net

# LiteLLM proxy admin UI — used to auto-login from the model launcher.
# NOTE: NEXT_PUBLIC_* values are embedded in the browser bundle, so treat
# these as convenience creds for a trusted local/LAN deployment only.
NEXT_PUBLIC_LITELLM_URL=http://localhost:4001
NEXT_PUBLIC_LITELLM_USERNAME=admin
NEXT_PUBLIC_LITELLM_PASSWORD=your-litellm-master-key
