# ComfyUI Integration Placeholder

Planned responsibilities:

- Async image/video generation execution
- Worker-safe job handoff and status polling
- Artifact output to sharded storage path

Expected worker binding in backend: `app/workers/media_worker.py`.
